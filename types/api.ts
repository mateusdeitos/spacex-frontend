
export namespace ApiTypes {

	export type TLatestLaunchSummary = {
		missionName: string;
		missionDate: string;
		flightNumber: number;
	}

	export type TNextLaunchSummary = {
		missionName: string;
		missionDate: string;
		flightNumber: number;
	}
}
